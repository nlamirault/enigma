# machine:
#   services:
#     - docker

dependencies:
  override:
    # - docker info
    - go version
    - make init

test:
  override:
    - make build
    - make test
    - make lint
    - make vet
    - make coverage
