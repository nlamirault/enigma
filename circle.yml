# machine:
#   services:
#     - docker

dependencies:
  pre:
    - rm -f /home/ubuntu/.gitconfig
  override:
    - go version
    - go get github.com/constabulary/gb/...
    - go get github.com/golang/lint/golint

test:
  override:
    - make init
    - make build
    - make test
    - make lint
    - make vet
    - make coverage
