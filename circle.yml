# machine:
#   services:
#     - docker

dependencies:
  pre:
    - rm -f ~/.gitconfig
  override:
    - go version

test:
  override:
    - make init
    - make build
    - make test
    - make lint
    - make vet
    - make coverage
