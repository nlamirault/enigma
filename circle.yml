# machine:
#   services:
#     - docker

dependencies:
  override:
    - go version

test:
  override:
    - make init
    - make build
    - make test
    - make lint
    - make vet
    - make coverage
